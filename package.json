{
  "name": "ledgerlint",
  "description": "VS Code extension for ledgerlint",
  "author": "Takamasa Oshikiri",
  "license": "MIT",
  "version": "1.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/oshikiri/vscode-ledgerlint"
  },
  "publisher": "oshikiri",
  "categories": [
    "Linters"
  ],
  "keywords": [
    "multi-root ready"
  ],
  "engines": {
    "vscode": "^1.43.0"
  },
  "activationEvents": [
    "onLanguage:ledger"
  ],
  "main": "./client/out/extension",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "ledgerlint",
      "properties": {
        "ledgerlint.commandTemplate": {
          "scope": "resource",
          "type": "string",
          "default": "ledgerlint -j -f $(realpath --relative-to=. ${path})",
          "description": "command template (${path} will be replace by absolute path of focus file)"
        }
      }
    },
    "languages": [
      {
        "id": "ledger",
        "aliases": [
          "Ledger journal"
        ],
        "extensions": [
          ".ledger"
        ]
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "cd client && cd .. && npm run compile",
    "precompile": "npm run format-check && npm run lint",
    "compile": "tsc -b",
    "lint": "eslint **/*.ts",
    "watch": "tsc -b -w",
    "format": "prettier --write **/*.ts",
    "format-check": "prettier --check **/*.ts",
    "postinstall": "cd client && npm install && cd ../server && npm install && cd ..",
    "test": "cd server && npm test && cd - && sh ./scripts/e2e.sh"
  },
  "devDependencies": {
    "@types/mocha": "^9.0.0",
    "@types/node": "^12.11.7",
    "@typescript-eslint/parser": "^3.0.2",
    "eslint": "^7.1.0",
    "mocha": "^9.1.3",
    "prettier": "^2.5.1",
    "typescript": "^3.9.3"
  }
}
